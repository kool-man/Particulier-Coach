<div class="col-md-3 col-sm-6">
            <h3 style="font-size:18px;">NAVIGATION</h3>
            <ul>
                {% for navigation in navigations %}
                    <li><a href="{{ navigation.lien }}" title="{{ navigation.titre }}" {% if navigation.titre == 'Blog' %}target="_blank"{% endif %}>{{ navigation.titre }}</a></li>
                {% endfor %}
            </ul>    
</div>
